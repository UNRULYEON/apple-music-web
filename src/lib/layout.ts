// the room every screen keeps at its sides. A section title, a track name and a tile all
// start here, so they line up down the screen.
export const VIEW_INSET = "px-6 sm:px-8";

// what a track row takes back out of that room, which is the padding the row itself
// carries for its highlight. The words on a row then start where a title above them does.
export const ROW_BLEED = "-mx-2 sm:-mx-4";

export const BAR_INSET = "ps-4 pe-6 sm:ps-6 sm:pe-8";
